#ifndef FECHA_H_INCLUDED
#define FECHA_H_INCLUDED
#include <stdio.h>
#include "Boolean.h"

typedef struct {int dia;
                int mes;
                int anio;
                } Fecha;


#endif // FECHA_H_INCLUDED
